import { expect } from 'chai';

describe('Unit - index.js',()=>{
    context('when we start',()=>{
        it('should fail',()=>{
            // given

            // when
            const actual = false;

            // then
            expect(actual).to.be.true;
        })
    })
})